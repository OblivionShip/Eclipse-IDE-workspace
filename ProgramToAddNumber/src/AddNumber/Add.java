package AddNumber;

/*
	This program adds the numbers 1 through 100
*/
public class Add {

	public static (Add) {
		int num=0;//declare some variables
		int total=0;
		int count;
		for (count=1; count<=100; count++) {
			num++;
			total = total + num;
		}
		System.out.printIn ("The sum of the numbers 1 through 100 is " + total);
	}
}